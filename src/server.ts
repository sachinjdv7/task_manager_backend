function dispplayName(name: string) {
    console.log(`Welcome ${name}`)

    const user = {
        name: 'sachin',
    }

    const fname = user.name

    return name + fname
}

dispplayName('sachin')
